# Dependencias del cliente desktop LBAMonitor v4.3 (PySide6).
#
# Nota: el código usa PySide6 (no PyQt6). Ambos son bindings Python de Qt6
# con APIs casi idénticas. La auditoría mencionaba PyQt6 pero el código
# histórico (v4.0.0, v4.2, v4.3) usa PySide6, así que se mantiene por
# consistencia. Si se quisiera migrar a PyQt6, los cambios son mecánicos:
#   from PySide6.QtXxx → from PyQt6.QtXxx
#   Signal → pyqtSignal
#   QDialog.DialogCode.Accepted → QDialog.Accepted
# (y cualificar enums: Qt.AlignmentFlag.AlignCenter en lugar de Qt.AlignCenter).

PySide6>=6.6.0
requests>=2.31.0
websocket-client>=1.7.0
PyInstaller>=6.4.0
